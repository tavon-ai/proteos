# A2 Configuration
# Generated by: a2 add
# Profile: api
# Target: poc
#
# Run checks with: a2 check --profile api --target poc

language:
    explicit:
        - node
files:
    required:
        - README.md
coverage:
    threshold: 40
