[defaults]
inventory = inventory.ini
roles_path = roles
host_key_checking = False
retry_files_enabled = False
stdout_callback = default
result_format = yaml
# The node-agent host is a single KVM VM; keep runs serial and verbose-friendly.
forks = 5

[privilege_escalation]
become = True
become_method = sudo
