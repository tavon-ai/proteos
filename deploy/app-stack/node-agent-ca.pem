# Placeholder — replace with the node-agent's cert (TAV-27 TLS pin).
#
# The ansible node_agent role fetches it to
# deploy/ansible/artifacts/node-<host>-agent-ca.pem; copy that file over this
# one and set PROTEOS_NODE_CA_FILE=/etc/proteos/node-agent-ca.pem in .env.
# While PROTEOS_NODE_CA_FILE is unset this file is never read.
